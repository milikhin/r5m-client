<script>
  require.config({
    baseUrl: '/',
    paths: {
        text: "bower_components/text/text"
    }
  });

  require(['engine/lp'], function () {
    require(['r5m/app'], function (app) {
      app(window.r5m.modules);
    });
  });

  (function () {
    if (window.r5m.app.EMAILJS_ID) {
      window.emailjs.init(window.r5m.app.EMAILJS_ID);
    }
  })();
</script>
